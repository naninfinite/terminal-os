import React from 'react';

const AboutWindow: React.FC = () => (
  <div>
    <div style={{ marginBottom: 6 }}>ABOUT.TXT</div>
    <p>TERMINAL-OS — retro terminal UI.</p>
  </div>
);

export default AboutWindow;


